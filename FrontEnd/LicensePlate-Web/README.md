# 📊 React Data Dashboard

## ✨ Project Goals

A modern, responsive dashboard application built with **React** and **Vite**. This project focuses on displaying and visualizing data (likely fetched from a backend API) using **Recharts** and implementing dynamic client-side routing.

## 📦 Key Technologies

* **Frontend Library:** React (via Vite)
* **Routing:** `react-router-dom`
* **Styling:** Tailwind CSS, Bootstrap 5
* **Data Visualization:** Recharts

---

## 💻 Getting Started

### Prerequisites

* Node.js (LTS version recommended)
* npm or yarn

### 1. Installation

Install all project dependencies using the commands provided:

```bash
# 1. Install Core Dependencies
npm install

# 2. Install Styling (Bootstrap)
npm i bootstrap@5.3.8

# 3. Install React Router for navigation
npm i react-router-dom

# 4. Install Tailwind CSS and its dependencies
npm install tailwindcss @tailwindcss/vite

# 5. Install Data Visualization Library
npm i recharts


# Running the Application
npm run dev
